# 双色球预测 Android APP — 完整开发提示词

## 1. 项目目标
开发一款纯本地、免联网的 Android 双色球预测 APK，基于 2003 年至今全部历史开奖数据，集成多种统计学与机器学习算法，提供号码推荐、走势分析、数据可视化功能。

> **免责声明：本 APP 仅供学习与娱乐参考，不构成任何购彩建议。彩票结果完全随机，任何预测算法均无法保证中奖。**

---

## 2. 技术架构

| 层级 | 方案 |
|------|------|
| 语言 | Kotlin 1.9+ |
| UI 框架 | Jetpack Compose + Material 3 |
| 架构模式 | MVVM + Repository + UseCase |
| 本地数据库 | Room（预置 2003~2026 全部历史数据） |
| 图表引擎 | Vico（Compose 原生图表库） |
| 异步 | Kotlin Coroutines + Flow |
| 依赖注入 | Hilt |
| 导航 | Compose Navigation |
| 构建 | Gradle Kotlin DSL + Version Catalog |
| 最低 SDK | 26 (Android 8.0) |
| 目标 SDK | 34 |
| 包名 | `com.ssq.predictor` |

---

## 3. 双色球规则速查

| 项目 | 规则 |
|------|------|
| 红球 | 1~33 选 6 个，不重复，不计顺序 |
| 蓝球 | 1~16 选 1 个 |
| 开奖日 | 每周二、四、日 |
| 奖级 | 6 个等级（一等奖：6红+1蓝全中） |

---

## 4. 功能模块

### 4.1 数据层
- Room 数据库存储所有历史开奖记录
- 预置 CSV 数据首次启动自动导入
- 支持手动增/删/改单期数据
- 支持从 `cwl.gov.cn` 解析最新开奖（可选在线更新）

### 4.2 预测算法（核心）

| 算法 | 原理 | 输出 |
|------|------|------|
| 热冷分析 | 统计最近 N 期频率，按权重抽样 | 5组号码 + 热度评分 |
| 遗漏分析 | 基于当前遗漏值 + 平均遗漏，推荐回补号码 | 5组号码 + 遗漏评分 |
| 马尔可夫链 | 一阶转移矩阵，基于上期预测下期 | 5组号码 + 转移概率 |
| 区间分布 | 按三区间(1-11/12-22/23-33)、奇偶比、大小比约束生成 | 5组号码 + 分布评分 |
| 集成预测 | 上述 4 种加权投票，生成综合推荐 | 5组号码 + 综合评分 |

**预测输出：**
- 每次生成 5 组推荐号码（6红+1蓝）
- 每组附带 0~100 综合评分
- 每个号码标注推荐理由标签（🔥热号 / ❄️冷号 / ⏳回补 / 📊均衡）

### 4.3 数据可视化
- 红球走势折线图（可选单号或全部）
- 遗漏柱状图（33个红球 + 16个蓝球）
- 冷热分布热力图
- 三区间出号比例饼图
- 奇偶比历史趋势图
- 和值/跨度分布散点图

### 4.4 号码筛选器
- 用户自定义红球必选/排除
- 蓝球必选/排除
- 和值范围（常见 70~140）
- 跨度范围（常见 18~32）
- 奇偶比例（如 3:3、4:2）
- AC 值范围
- 连号过滤
- 保存/加载策略模板

### 4.5 开奖提醒与比对
- 开奖日本地通知推送
- 输入自选号码后自动与开奖结果比对
- 自动判定中奖等级（一~六等）
- 中奖历史记录

### 4.6 回测系统
- 选择历史某一期，用之前数据预测，验证准确率
- 统计各算法历史命中率（如 2红、3红、4红、5红、6红 命中次数）
- 评分算法可靠性排名

### 4.7 UI/UX
- Material 3 设计语言
- 深色/浅色模式自适应
- 底部导航栏（首页 / 预测 / 图表 / 设置）
- 流畅过渡动画
- 横屏适配
- 中文全界面

---

## 5. 完整项目结构

```
SSQPredictor/
├── .github/workflows/build.yml
├── app/
│   ├── build.gradle.kts
│   ├── proguard-rules.pro
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── assets/ssq_history.csv
│       ├── res/
│       │   ├── values/strings.xml
│       │   ├── values/themes.xml
│       │   ├── drawable/ic_launcher_foreground.xml
│       │   └── mipmap-*/
│       └── java/com/ssq/predictor/
│           ├── SSQApplication.kt          # Hilt Application
│           ├── MainActivity.kt             # 单 Activity 入口
│           ├── di/
│           │   └── AppModule.kt            # Hilt 依赖模块
│           ├── data/
│           │   ├── local/
│           │   │   ├── entity/DrawEntity.kt
│           │   │   ├── dao/DrawDao.kt
│           │   │   └── db/SSQDatabase.kt
│           │   ├── repository/DrawRepository.kt
│           │   └── datasource/AssetDataSource.kt
│           ├── domain/
│           │   ├── model/
│           │   │   ├── Draw.kt
│           │   │   ├── PredictionSet.kt
│           │   │   ├── BallType.kt
│           │   │   └── FilterConfig.kt
│           │   ├── algorithm/
│           │   │   ├── BasePredictor.kt
│           │   │   ├── HotColdPredictor.kt
│           │   │   ├── OmissionPredictor.kt
│           │   │   ├── MarkovPredictor.kt
│           │   │   ├── DistributionPredictor.kt
│           │   │   └── EnsemblePredictor.kt
│           │   └── usecase/
│           │       ├── PredictUseCase.kt
│           │       └── VerifyPrizeUseCase.kt
│           └── ui/
│               ├── navigation/NavGraph.kt
│               ├── theme/
│               │   ├── Theme.kt
│               │   ├── Color.kt
│               │   └── Type.kt
│               ├── home/
│               │   ├── HomeScreen.kt
│               │   └── HomeViewModel.kt
│               ├── prediction/
│               │   ├── PredictionScreen.kt
│               │   └── PredictionViewModel.kt
│               ├── history/
│               │   ├── HistoryScreen.kt
│               │   └── HistoryViewModel.kt
│               ├── charts/
│               │   ├── ChartsScreen.kt
│               │   └── ChartsViewModel.kt
│               ├── settings/
│               │   ├── SettingsScreen.kt
│               │   └── SettingsViewModel.kt
│               └── components/
│                   ├── BallView.kt
│                   ├── ScoreBar.kt
│                   └── FilterPanel.kt
├── build.gradle.kts                     # 根项目
├── settings.gradle.kts
├── gradle.properties
├── gradle/
│   └── libs.versions.toml               # Version Catalog
└── gradlew / gradlew.bat
```

---

## 6. 依赖清单 (libs.versions.toml)

```toml
[versions]
kotlin = "1.9.22"
agp = "8.2.2"
compose-bom = "2024.02.00"
compose-compiler = "1.5.10"
hilt = "2.50"
room = "2.6.1"
navigation = "2.7.7"
vico = "2.0.0-alpha.19"
coroutines = "1.8.0"

[libraries]
compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "compose-bom" }
compose-ui = { group = "androidx.compose.ui", name = "ui" }
compose-material3 = { group = "androidx.compose.material3", name = "material3" }
compose-navigation = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }
hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }
hilt-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }
hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version = "1.2.0" }
room-runtime = { group = "androidx.room", name = "room-runtime", version.ref = "room" }
room-ktx = { group = "androidx.room", name = "room-ktx", version.ref = "room" }
room-compiler = { group = "androidx.room", name = "room-compiler", version.ref = "room" }
vico-compose = { group = "com.patrykandpatrick.vico", name = "compose-m3", version.ref = "vico" }
coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
ksp = { id = "com.google.devtools.ksp", version = "1.9.22-1.0.17" }
```

---

## 7. 提交信息规范
```
feat: 新功能
fix: 修复
refactor: 重构
docs: 文档
perf: 性能优化
test: 测试
chore: 构建/CI
```

---

## 8. CI/CD (GitHub Actions)
- 触发: push / PR → main
- 任务: `gradle assembleDebug` + lint 检查
- 产物: 上传 APK 到 Actions Artifact

---

## 9. 隐私与合规
- 不收集任何用户数据
- 无需网络权限（在线更新为可选功能）
- 启动时显示免责弹窗
- 符合国内应用商店基本规范
